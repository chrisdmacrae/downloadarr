{
  "name": "downloadarr",
  "version": "1.0.0",
  "description": "All-in-one media and ROM downloading tool with VPN integration",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "dev:build": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "dev:detached": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build -d",
    "dev:down": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml down",
    "dev:logs": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml logs -f",
    "dev:rebuild": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml down && docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "dev:vpn": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.dev.vpn.yml up",
    "dev:vpn:build": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.dev.vpn.yml up --build",
    "dev:vpn:detached": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.dev.vpn.yml up --build -d",
    "dev:vpn:down": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.dev.vpn.yml down",
    "dev:vpn:logs": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.dev.vpn.yml logs -f",
    "build": "npm run build --workspaces",
    "build:api": "npm run build --workspace=@app/api",
    "build:ui": "npm run build --workspace=@app/ui",
    "start": "npm run start --workspace=@app/api",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces && rm -rf node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.2.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chrisdmacrae/downloadarr.git"
  },
  "author": "Chris MacRae",
  "license": "MIT"
}
