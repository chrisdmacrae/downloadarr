// Runtime configuration for Downloadarr frontend
// This file is dynamically generated at container startup

(function() {
  // Default configuration
  window.__RUNTIME_CONFIG__ = {
    apiUrl: '/api'  // Default to nginx proxy
  };
  
  // Override with environment-specific configuration if available
  // This will be replaced by the container startup script
  // RUNTIME_CONFIG_PLACEHOLDER
})();
